import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full widget tests require Hive and async init — use integration tests instead.
    expect(true, isTrue);
  });
}
